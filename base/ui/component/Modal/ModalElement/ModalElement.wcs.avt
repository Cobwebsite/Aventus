:host {
	--_modal-z-index: var(--modal-z-index, 60);
}

:host {
	align-items: center;
	display: flex;
	inset: 0;
	justify-content: center;
	position: fixed;
	z-index: var(--_modal-z-index);

	.modal {
		background-color: white;
		padding: 1.5rem;
		position: relative;
	}
}
